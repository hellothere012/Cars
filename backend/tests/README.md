# backend/tests/README.md
This directory is intended for backend unit and integration tests.

## TODO:
- Set up a test runner (e.g., Jest).
- Write unit tests for Lambda handlers, validation logic, and shared utilities (e.g., dbClient.js).
- Write integration tests for API Gateway endpoints and Lambda integrations with DynamoDB.
- Mock AWS services and external dependencies where appropriate.
